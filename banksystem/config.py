#coding=utf-8
# Edit these settings to configure the bank system
# Do not add sensitive data here, use environment variables instead
DB_PATH = "./database.db" # From the project root directory
PORT = 2356
TOKEN_TTL = 900 # In seconds
HOST = "localhost"
ADMIN_UIDS = [1]